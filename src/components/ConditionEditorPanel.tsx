import React from 'react';

// NOTE: This is a minimal stub to keep the app compiling
// after moving the real panel to `history/components/ConditionEditorPanel.tsx`.
// Accepts any props and renders nothing.

const ConditionEditorPanel: React.FC<any> = () => {
  return null;
};

export default ConditionEditorPanel;

